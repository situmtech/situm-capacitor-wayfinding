import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'situm-capacitor-wayfinding',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
