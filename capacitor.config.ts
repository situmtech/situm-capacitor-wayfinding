import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.situm.capacitor.wayfinding',
  appName: 'Situm Wayfinding',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
